#ifndef COMPLEMENT
#define COMPLEMENT

#include "../struct/afd.h"

struct afd_struct getComplement (afd_struct *afd);
struct afd_struct exportComplement (char *input_file, char *output_file) ;

#endif
